{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2",
    "microbundle": "^0.11.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn lerna run --parallel --no-bail start"
  }
}
